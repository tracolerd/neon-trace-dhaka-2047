# Assets

## Developer profile

- `assets/developer/nurul-faiyaz.jpg` — developer profile photo supplied with the project; loaded by `scenes/ui/DeveloperInfo.tscn`.

Temporary geometry and UI must be marked with `TEMP_` names. Any later external asset must include its source, license, author and integration path here.
