# Assets

No external assets are included yet. Temporary geometry and UI must be marked with `TEMP_` names. Any later external asset must include its source, license, author and integration path here.
