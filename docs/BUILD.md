# Build

Target platforms are Windows and Linux. Use a stable Godot 4.x release and export with the project renderer configured in `project.godot`. Export presets will be added before release builds.
