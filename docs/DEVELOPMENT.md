# Development

Use Godot 4.x consistently. Keep systems focused, data-driven and signal-based. Build one coherent milestone at a time; do not add campaign content until the vertical slice is complete.

## Naming

GDScript files and variables use `snake_case`; scenes and classes use `PascalCase`; constants use `UPPER_CASE`.
