class_name PasswordInference
extends HackPuzzleBase

func _init() -> void:
	title = "Password Inference"
	prompt = "Infer the credential pattern from the prior breach log."
	answer = "NEXUS"
	hint = "This was the earlier project alias used by the operators."
