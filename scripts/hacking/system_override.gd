class_name SystemOverride
extends HackPuzzleBase

func _init() -> void:
	title = "System Override"
	prompt = "Override the lock protocol before the system resets."
	answer = "OVERRIDE"
	hint = "The command key is the same as the system's default override phrase."
